import { PlaceholderPage } from "@/components/PlaceholderPage";

export function SearchPage() {
  return (
    <PlaceholderPage
      title="Search chats"
      description="Web search is a chat tool (globe in the composer). Conversation search is not part of v1 — use the sidebar filter for titles."
    />
  );
}
